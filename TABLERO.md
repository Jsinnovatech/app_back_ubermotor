# Tablero — App Back UberMotor

## Por Hacer
- [ ] **Separación de capas en `conductores.py`**: mover la lógica de negocio de `subir_documento`, `actualizar_perfil`, `cambiar_disponibilidad` y `actualizar_ubicacion` a un `conductor_service.py` nuevo (siguiendo el mismo patrón que ya usa `viaje_service.py`: router delgado → servicio con la lógica → commit adentro del servicio, no en el router). Subir evidencia (diff o captura) del refactor aquí cuando esté listo.

## En Progreso
- (vacío)

## Hecho
- (vacío)
